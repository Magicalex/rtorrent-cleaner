#!/usr/bin/env php
<?php

if (file_exists(__DIR__.'/../vendor/autoload.php')) {
    require __DIR__.'/../vendor/autoload.php';
} else {
    require __DIR__.'/../../../autoload.php';
}

use Symfony\Component\Console\Application;
use RtorrentCleaner\Command\moveCommand;
use RtorrentCleaner\Command\RemoveCommand;
use RtorrentCleaner\Command\ReportCommand;

// init
$app = new Application('Rtorrent Cleaner', '0.2.0');

// add commands
$app->add(new ReportCommand());
$app->add(new RemoveCommand());
$app->add(new moveCommand());

$app->run();
